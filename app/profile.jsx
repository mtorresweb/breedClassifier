import React from "react";
import { View, Text } from "react-native";

const Profile = () => {
	return (
		<View>
			<Text>Welcome to the Profile component!</Text>
		</View>
	);
};

export default Profile;
